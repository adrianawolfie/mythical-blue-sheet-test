# Latest characters for the Mythical Blue test repository

Copy the `characters/` folder contents into the test repository's `characters/` folder.

Included:
- `test-character-index.json`
- Nimue
- Crowley
- Vasir Serpentis
- Lillith
- Mara Flint

Do not replace `characters/character-index.json` with the production index for this test update.
The local/GitHub Pages test environment seeds from `characters/test-character-index.json`.

After deploying the updated files, use the test app's Reset Test Data action (or clear the app's local storage) so the browser reloads the updated seed JSON files.
